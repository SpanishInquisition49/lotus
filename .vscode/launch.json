{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "lldb",
      "name": "lotus",
      "request": "launch",
      "program": "${workspaceFolder}/lotus",
      "cwd": "${workspaceFolder}",
      "args": [
          "${workspaceFolder}/test/arithmetic.lotus"
      ]
    }
  ]
}